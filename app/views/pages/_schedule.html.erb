<% event_schedule = Schedule.new("1fiOl0WWDPlPMLhZfM2DTJDJZ8UBzwhAk0FHMAogg6lY", 0) %>
<% sections = event_schedule.sections %>
<div id="schedule" class="hash-anchor"></div>
<div class="section section--white">
  <div class="section__content">
    <h2 class="pull-into-squig">Schedule</h2>
    <% if sections.blank? %>
      <p><strong><span class="fa fa-clock-o icon-space-r"></span>Schedule temporarily unavailable - please check back later!</strong></p>
      <br/>
    <% else %>
      <div class="split">
        <% sections.each do |section| %>
          <div class="split__section split__section--wide">
            <h3 class="h2--like"><%= section.shift %></h2>
            <% section.each do |row| %>
              <% if row[0].present? %>
                <p class="margin-after"><strong><%= row[0] %></strong> - <%= row[1] %></p>
              <% else %>
                <p class="margin-after"><em><%= row[1] %></em></p>
              <% end %>
            <% end %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
